services:
  api1: &api
    image: ghcr.io/pedrosakuma/rinha-backend-2026-gpu-bruteforce:latest
    platform: linux/amd64
    command:
      - --serve
      - --backend
      - vulkan
      - --listen
      - 0.0.0.0:8080
      - --skip-validate
      - --require-resource-files
      - --references-path
      - /app/resources/references.json.gz
      - --mcc-risk-path
      - /app/resources/mcc_risk.json
      - --normalization-path
      - /app/resources/normalization.json
    volumes:
      - type: bind
        source: /dev/dri
        target: /dev/dri
    device_cgroup_rules:
      - "c 226:* rmw"
    deploy:
      resources:
        limits:
          cpus: "0.40"
          memory: "160M"

  api2:
    <<: *api

  lb:
    image: ghcr.io/pedrosakuma/rinha-backend-2026-gpu-bruteforce-lb:latest
    platform: linux/amd64
    depends_on:
      - api1
      - api2
    ports:
      - "9999:9999"
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "30M"
